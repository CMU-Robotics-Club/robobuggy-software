SC:
  SC_sim_single:
    ros__parameters:
      velocity: 12
      steering_offset: -5 # between 20 and -20, in degrees
      steering_delay: 0 # 1 step = 0.01s (at 100 hz steering)
      step_noise_std: 0.0 # standard deviation of simulated GPS noise (in meters) 
      pose: 'Hill1_SC'

  SC_controller:
    ros__parameters:
      dist: 0.0
      traj_name: 'buggycourse_safe.json'
      controller: 'stanley'
